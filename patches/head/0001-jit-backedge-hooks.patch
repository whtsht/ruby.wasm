diff --git a/insns.def b/insns.def
index 9705b33..a2243a5 100644
--- a/insns.def
+++ b/insns.def
@@ -1224,6 +1224,9 @@ jump
 // attr bool leaf = false; /* has rb_threadptr_execute_interrupts() */
 {
     RUBY_VM_CHECK_INTS(ec);
+    if (dst < 0) {
+        backedge((unsigned int)(GET_PC() + (dst)));
+    }
     JUMP(dst);
 }
 
@@ -1238,6 +1241,9 @@ branchif
 {
     if (RTEST(val)) {
         RUBY_VM_CHECK_INTS(ec);
+        if (dst < 0) {
+            backedge((unsigned int)(GET_PC() + (dst)));
+        }
         JUMP(dst);
     }
 }
@@ -1253,6 +1259,9 @@ branchunless
 {
     if (!RTEST(val)) {
         RUBY_VM_CHECK_INTS(ec);
+        if (dst < 0) {
+            backedge((unsigned int)(GET_PC() + (dst)));
+        }
         JUMP(dst);
     }
 }
@@ -1268,6 +1277,9 @@ branchnil
 {
     if (NIL_P(val)) {
         RUBY_VM_CHECK_INTS(ec);
+        if (dst < 0) {
+            backedge((unsigned int)(GET_PC() + (dst)));
+        }
         JUMP(dst);
     }
 }
diff --git a/vm.c b/vm.c
index ad8a22e..80962ab 100644
--- a/vm.c
+++ b/vm.c
@@ -1749,6 +1749,7 @@ rb_binding_add_dynavars(VALUE bindval, rb_binding_t *bind, int dyncount, const I
 static inline void
 invoke_block(rb_execution_context_t *ec, const rb_iseq_t *iseq, VALUE self, const struct rb_captured_block *captured, const rb_cref_t *cref, VALUE type, int opt_pc)
 {
+    backedge((unsigned int)ISEQ_BODY(iseq)->iseq_encoded);
     int arg_size = ISEQ_BODY(iseq)->param.size;
 
     vm_push_frame(ec, iseq, type | VM_FRAME_FLAG_FINISH, self,
diff --git a/vm_insnhelper.h b/vm_insnhelper.h
index 88c387e..926ce66 100644
--- a/vm_insnhelper.h
+++ b/vm_insnhelper.h
@@ -11,6 +11,14 @@
 
 **********************************************************************/
 
+/* JIT hotspot detection: imported from Wasm host on wasm32-wasi */
+#if defined(__wasm__) && defined(__wasi__)
+__attribute__((import_module("env"), import_name("backedge")))
+extern void backedge(unsigned int pc);
+#else
+static inline void backedge(unsigned int pc) { (void)pc; }
+#endif
+
 RUBY_EXTERN VALUE ruby_vm_const_missing_count;
 RUBY_EXTERN rb_serial_t ruby_vm_constant_cache_invalidations;
 RUBY_EXTERN rb_serial_t ruby_vm_constant_cache_misses;
